services:
  loucherbem-php:
    image: php:8.3-cli
    container_name: loucherbem
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    working_dir: /app
    command: php -S 0.0.0.0:8000 -t /app/public